# === Основное приложение (Flask 3.0+) ===
Flask==3.0.3
Jinja2==3.1.4
Werkzeug==3.0.4
itsdangerous==2.2.0
MarkupSafe==2.1.5
click==8.1.7

# === Безопасность и окружение ===
python-dotenv==1.0.1
Flask-Talisman==1.1.0          # очень рекомендуется на Render (заголовки безопасности)

# === HTTP-клиент (Gemini, Exolve и т.д.) ===
httpx==0.27.2

# === Продакшн-сервер (обязательно для Render Web Service) ===
gunicorn==23.0.0
gevent==24.10.3                # сильно ускоряет gunicorn под нагрузкой

# === Логирование ===
colorlog==6.8.2                # если используешь цветные логи (можно убрать, если не нужен)

# === Утилиты (по желанию, но полезно) ===
watchdog==5.0.3                # для flask run --debug в локальной разработке
rich==13.9.4                   # красивый вывод ошибок и логов (необязательно)

# === Основной сайт (Flask) ===

Flask-WTF==1.2.1
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Talisman==1.1.0
Jinja2==3.1.4
Werkzeug==3.0.4
itsdangerous==2.2.0
MarkupSafe==2.1.5
pypdf>=6.0.0

# === AI-прокси (отдельный процесс на порту 8000) ===
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2             

# === Общие утилиты ===
requests==2.32.3
python-dotenv==1.0.1
colorlog==6.8.2
gunicorn==23.0.0             # для продакшена основного сайта

# === Если в будущем перейдёшь с чистого sqlite3 на SQLAlchemy (очень рекомендуется) ===
# Раскомментируй эти строки, когда будешь готов:
# Flask-SQLAlchemy==3.1.1
# Flask-Migrate==4.1.0
# Flask-WTF==1.2.1

# === Если хочешь отдельный AI-прокси на FastAPI (пока у тебя всё в одном процессе — НЕ НУЖНО) ===
# fastapi==0.115.2
# uvicorn[standard]==0.32.0